extends Node3D

# Called when the node enters the scene tree for the first time.
func _ready():
	pass

# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(_delta):
	pass

func _on_road_builder_construct_road(params):
	#var START: Vector3 = params.pop_front()
	#var MIDWAY: Vector3 = params.pop_front()
	#var END: Vector3 = params.pop_front()
	pass
